# Frontend Environment Variables

# Backend API URL (change to your deployed backend URL for production)
VITE_BACKEND_URL=http://localhost:8069

# Optional: Environment indicator
VITE_ENVIRONMENT=development

# Development note: In development, the proxy is used instead of VITE_BACKEND_URL
# In production builds, VITE_BACKEND_URL is used for direct API calls